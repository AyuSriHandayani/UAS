<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Activity Beranda" >

    <Button
        android:id="@+id/tambahResep"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/postingan"
        android:layout_alignLeft="@+id/resep"
        android:layout_alignRight="@+id/resep"
        android:text="Tambah Resep" />

    <Button
        android:id="@+id/resep"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tambahResep"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="20dp"
        android:text="Daftar Resep" />

    <Button
        android:id="@+id/postingan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tambahResep"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/tambahResep"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="121dp"
        android:text="Postingan Terakhir" />

</RelativeLayout>